server:
  port: 8082
spring:
  application:
    name: verification
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/verification
    username: lliu
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true